# ──────────────────────────────────────────────────────────────────────────
# Music Manager Dashboard — Dépendances Streamlit Cloud
# Déposer ce fichier à la racine du dépôt GitHub (même niveau que app.py).
# Streamlit Cloud l'exécute automatiquement via pip install -r requirements.txt
# avant le premier démarrage de l'application.
# ──────────────────────────────────────────────────────────────────────────

# Interface & serveur web
streamlit>=1.35.0

# API Spotify — OBLIGATOIRE (corrige le ModuleNotFoundError: No module named 'spotipy')
spotipy>=2.23.0

# Manipulation de données
pandas>=2.2.0

# Visualisations interactives
plotly>=5.22.0
